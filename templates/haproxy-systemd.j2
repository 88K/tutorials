[Unit]
Description={{haproxy_description}}
After=network.target

[Service]
Type=forking
WorkingDirectory={{haproxy_work_dir}}
ExecStart={{haproxy_exec_start}} -f {{haproxy_exec_cfg}}
Restart=on-failure
# Other Restart options: or always, on-abort, etc

[Install]
WantedBy=multi-user.target