[Unit]
Description={{nodexporter_description}}
After=network.target

[Service]
Type=simple
WorkingDirectory={{nodexporter_work_dir}}
ExecStart={{nodexporter_exec_start}}
Restart=on-failure
# Other Restart options: or always, on-abort, etc

[Install]
WantedBy=multi-user.target